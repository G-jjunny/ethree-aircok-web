# 서버 컴포넌트 → NestJS 직접 호출 URL (NEXT_PUBLIC_ 없음 — 서버에서만 읽힘)
# Next.js rewrites는 브라우저 요청에만 적용되므로, 서버 컴포넌트는 NestJS를 직접 가리켜야 함
API_URL=http://localhost:3001/api

# 배포 환경에서만 설정 (로컬 개발 시 불필요)
# API_URL=https://api.your-domain.com/api
